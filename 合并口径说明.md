# Q2 结果合并口径说明（2023 CUMCM C 题）

## 1. 为什么要合并

之前存在**两套口径不一致**的 Q2 结果，论文里若混用会被评委抓出矛盾：

| 来源 | 脚本 | 需求弹性模型 | R² | 预测模型 |
|---|---|---|---|---|
| `results/q2_model/` | `q2_model_improved.py` | **合并双对数**（`ln_Q ~ ln_p*品类 + ln_w + 月/周/年哑变量`） | **0.711** | XGBoost |
| `q2_output/` | `q2_category_forecast.py` | **逐品类双对数**（`lnq ~ lnp + lnw + 星期哑变量`） | 0.106~0.309 | LSTM |

两套模型给出的价格弹性差异很大（如水生根茎类 −1.392 vs −2.900、食用菌 −0.862 vs −1.162），决策表实际采用的是**较弱的那套**。

## 2. 合并策略（本次已执行）

- **销量预测基准**：保留 `q2_output` 的 **LSTM 逐品类 7 日预测**（即 `q2_补货定价决策表.csv` 的"基准需求"列）。原因：LSTM 预测与实际 2023-06 品类日均销量量级吻合（水生根茎类 15.7 vs 实际 16.9，茄类 25.3 vs 实际 25.6 等）；而 XGBoost 外推的品类总量系统性偏高 1.9~5.8 倍，不可作为补货基准。
- **需求价格弹性**：统一采用 `q2_model_improved.py` 的**合并双对数模型**（R²=0.711，n=6474），弹性标准误由 delta 法求得。
- **定价规则**：
  - ε < −1（富有弹性，仅水生根茎类）→ 内部最优加成率 `m* = −1/(1+ε)` 与历史加成率 6:4 加权，钳制 [10%, 100%]。
  - ε ∈ [−1, 0)（缺乏弹性，其余五类）→ **维持历史平均加成率**，不再用 95% 分位外推（修正了原 `q2_output` 把"无法确定最优价"误当"可大幅加价"的问题）。
- **收益口径**：边际销售成本 `MC = 批发成本/(1−损耗率)`；`收益 = 优化后需求 × (零售价 − MC)`；`补货量 = 优化后需求/(1−损耗率)`。

## 3. 合并口径下的验证评级（按你给的提示词）

弹性模型（合并双对数）R²=0.711、调整R²=0.710、Δ=0.0014、n=6474：

- **R² 维度**：R²=0.711 < 0.8 → 未达"良好"阈值；但 Δ=0.0014 ≪ 0.05，说明**不复杂、不过拟合**，提示词里的"可能过度复杂"提醒在此不成立，问题在需求-价格关系本身解释力中等。
- **残差维度**：残差均值各分箱≈0（无系统性趋势/曲线），但残差标准差随拟合值有 ~1.85 倍的收缩（低端 0.64 → 高端 0.35），Breusch-Pagan p<0.001 → **存在轻微异方差（漏斗）**，评级 **B**（轻微趋势，说明原因：大类销量 lnQ 的水平越高、需求噪声占比越低，属于 log-log 建模的常见形态，不构成终止模型的程度）。

**结论：合并口径下弹性模型评级 B，不终止使用，但需在论文里如实标注"R²=0.711、存在轻度异方差"。**

## 4. 合并口径最终结果

### 4.1 品类弹性与定价

| 品类 | 需求价格弹性 ε | 弹性 p 值 | 历史加成率 | 建议加成率 | 建议零售价(元/kg) |
|---|---|---|---|---|---|
| 水生根茎类 | −1.392 | <0.001 | 0.560 | 1.000 | 22.68 |
| 花叶类 | −0.606 | <0.001 | 0.782 | 0.782 | 5.58 |
| 花菜类 | −0.952 | <0.001 | 0.587 | 0.587 | 12.55 |
| 茄类 | −0.730 | <0.001 | 0.672 | 0.672 | 7.86 |
| 辣椒类 | −0.726 | <0.001 | 0.926 | 0.926 | 7.12 |
| 食用菌 | −0.862 | <0.001 | 0.674 | 0.674 | 7.30 |

### 4.2 决策周（2023-07-01~07-07）7 日汇总

| 品类 | 7日补货量(kg) | 7日收益(元) |
|---|---|---|
| 水生根茎类 | 98.5 | 882 |
| 花叶类 | 1332.8 | 2548 |
| 花菜类 | 206.4 | 651 |
| 茄类 | 179.0 | 466 |
| 辣椒类 | 527.8 | 1490 |
| 食用菌 | 191.7 | 451 |

**全品类 7 日合计：补货量 2536.2 kg，收益 6488 元。**

## 5. 输出文件

- `q2_补货定价决策表_合并口径.csv` —— 决策周逐品类逐日补货量 + 定价 + 收益（合并口径）
- `q2_需求定价关系表_合并口径.csv` —— 各品类弹性、显著性、加成率、定价规则
- `合并弹性模型_验证.json` —— 合并模型 R²/调整R²/BP 异方差/各品类弹性
- `验证_残差图_合并弹性模型.png` —— 合并模型残差诊断图
- `验证_残差图_决策表需求模型.png` —— 原逐品类模型残差图（保留，供对比）

## 6. 遗留风险（需人确认）

1. **合并弹性模型 R²=0.711 未达 0.8**，作为定价依据属于"可用但需保守表述"，论文不应写成"高精度刻画"。
2. **只有水生根茎类有内部最优价**，其余五类"维持历史加成率"意味着定价优化几乎没有动作空间——这是数据本身的结论，但评委可能追问"收益最大化"体现在哪里，需要准备解释（收益最大化主要体现在补货量精准化 + 水生根茎类提价）。
3. **异方差未消除**，如需彻底过关可对 lnQ 做加权最小二乘（WLS），但会改变弹性数值，需重新走一轮验证。
